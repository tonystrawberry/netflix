
<% if @display_as_button %>
  <%= link_to @text, @link, { class: "rounded-md bg-red-600 text-white px-3.5 py-2.5 text-sm font-semibold text-gray-900 shadow-sm hover:bg-red-800 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white #{@classes}" }.merge(@options) %>
<% else %>

<% end %>
