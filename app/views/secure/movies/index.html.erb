<!--
  This example requires some changes to your config:

  ```
  // tailwind.config.js
  module.exports = {
    // ...
    plugins: [
      // ...
      require('@tailwindcss/aspect-ratio'),
    ],
  }
  ```
-->


<!--
  This example requires updating your template:

  ```
  <html class="h-full">
  <body class="h-full">
  ```
-->
<div class="min-h-full">
  <%= render(Secure::HeaderComponent.new(profile: @current_profile)) %>

  <div class="py-10 space-y-8">
    <% @movies.each do |genre, movies| %>
      <section>
        <div class="mx-auto px-4 sm:px-6 lg:px-8">
          <h1 class="text-3xl font-bold leading-tight tracking-tight text-gray-900"><%= genre.name %></h1>
        </div>

        <div class="mt-4 px-4 sm:px-6 lg:px-8 overflow-hidden">
          <div class="whitespace-nowrap">
            <% movies.each do |movie| %>
              <%= link_to movie_path(movie.id), class: "inline-block w-60 h-32 overflow-hidden rounded" do %>
                <%= image_tag movie.cover, class: "w-full h-full object-cover object-center", alt: movie.title %>
              <% end %>
            <% end %>
          </div>
        </div>
      </section>
    <% end %>
  </div>
</div>
